import 'package:flutter/material.dart';

class AppColors {
  // Base colors using HSL (Hue, Saturation, Lightness)
  static const primary = Color(0xFF2E7D32);

  static const secondary = Color(0xFFB71C1C);

  static const bgTwo = Color(0xFFfafafa);

  static const bgOne = Color(0xFFebebeb);

  static const border = Color(0xFF7a7a7a);

  static const text = Color(0xFF040404);

  static const mutedText = Color(0xFF828282); // near white
}
